import './app/app';

/* npx create-nx-workspace flappy-bird --preset=apps --nxCloud=skip

npm run nx add @nx/web

npm run nx g @nx/web:app -- --name=flappy-bird --directory=apps/flappy-bird \
--bundler=vite --unitTestRunner=vitest \
--tags=scope:browser --style=scss --strict=true --linter=eslint --e2eTestRunner=playwright

npm i -D @babylonjs/inspector gh-pages sass-embedded type-fest

npm i -S \
@babylonjs/core \
inversify miniplex \
@babylonjs/loaders \
@babylonjs/gui \
@babylonjs/materials \
@babylonjs/havok 

update .gitignore
env.*
*.env */

